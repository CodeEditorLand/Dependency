{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"Document": {
			"dependsOn": ["^Document"],
			"outputs": ["Documentation/**"]
		},
		"Build": {
			"dependsOn": ["^Build"],
			"outputs": ["Target/**"]
		}
	}
}
